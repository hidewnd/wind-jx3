server:
  port: 9001
spring:
  application:
    name: costing
  profiles:
    include: dev
  cache:
    live-time: 180000  # 缓存过期时间（单位：ms）
    type: redis
    redis:
      cache-null-values: true  # 允许缓存空值，防止缓存穿透
  data:
    redis:
      host: 192.168.3.4
      port: 6379
      database: 4
      lettuce:
        pool:
          min-idle: 0
          max-idle: 8
          max-wait: -1ms
          max-active: 6

knife4j:
  enable: true
  setting:
    language: zh_cn
  #开启生产环境屏蔽
  production: false
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.hidewnd.costing.controller
box:
  default:
    server: '剑胆琴心'
jx3api:
  url: 'https://www.jx3api.com'
  token: ''
  ticket: ''  